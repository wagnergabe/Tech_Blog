
<div class="card w-80 p-2 mb-2">
  <div>
    {{#each this}}
      <section>
        <div class="comments">
          {{user.username}}
          on
          {{format_date created_at}}
        </div>
        <div>
          {{comment_text}}
        </div>
      </section>
    {{/each}}
  </div>
</div>